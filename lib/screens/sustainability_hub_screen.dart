// sustainability_hub_screen.dart
import 'package:flutter/material.dart';

/// User profile and sustainability  page.
/// Assigned to: Camus
/// Focus here is on the UI styling and layout of the user's stats and history later on pa
class SustainabilityHubScreen extends StatelessWidget {
  const SustainabilityHubScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(
        // Placeholder for the profile/hub dashboard
        child: Text(
          'Profile Page ni Camus (UI Styling)',
          style: TextStyle(fontSize: 18),
        ),
      ),
    );
  }
}